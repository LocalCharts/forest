\date{2024-04-25}
\import{macros}
\author{eigil-rischel}
\import{macros}
\taxon{definition}
\title{Convex conjugate}
\p{
    Let #{V} be a (real) vector space, and #{f: V \to \RR} be a function (not necessarily linear).
    Then the \em{convex conjugate} #{f^*: V^* \to \RR} is defined by
    ##{f^*(\alpha) = \sup_x \alpha(x) - f(x)}
}